# dependencies (bun install)
node_modules

# output
dist

# code coverage
coverage
*.lcov

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.cache
*.tsbuildinfo

# Astro's own build cache
.astro

# wrangler's local state — auth, deploy history
.wrangler

# Playwright's own run output
test-results
playwright-report
blob-report

# lhci's own run output
.lighthouseci

# downloaded by bun run prose:sync — the Vale binary, and the style packages it
# lints against. styles/config is ours and stays committed, so the package
# directories are ignored by name rather than the whole styles/ tree
.tools
styles/Google
styles/proselint
