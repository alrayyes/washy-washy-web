[test]
# Compiles `.svelte` imports to their server-rendered output before `bun
# test` evaluates them — see the preload file's own comment for why this
# is needed and why it's scoped to `[test]` alone (#243).
preload = ["./test/support/svelteServer.ts"]
